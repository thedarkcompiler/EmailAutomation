# 1. Install deps
`pip install -r requirements.txt`

# 2. Test app
`python main.py`

# 3. Build executable
`pyinstaller --onefile --windowed --name="StratoMail" main.py`

# 4. Find your app in:
#    Windows → dist/StratoMail.exe
#    Mac     → dist/StratoMail.app